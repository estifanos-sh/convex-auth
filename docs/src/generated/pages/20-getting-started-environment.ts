import type { DocumentationPage } from "../docs";

const page = {
  description: "Required and optional environment variables for convex-auth.",
  html: '<h1 id="environment-variables">Environment Variables</h1>\n<h2 id="required">Required</h2>\n<p>| Variable                     | Purpose                                                                       |\n| ---------------------------- | ----------------------------------------------------------------------------- |\n| <code>JWT_PRIVATE_KEY</code>            | Signs session JWTs                                                            |\n| <code>JWKS</code>                       | JSON Web Key Set for verification                                             |\n| <code>AUTH_SECRET_ENCRYPTION_KEY</code> | Encrypts stored OIDC client secrets and group webhook signing secrets at rest |\n| <code>APP_URL</code>                    | Frontend URL for OAuth, email, device, and passkey defaults                   |</p>\n<p>These are set automatically by the CLI setup wizard.</p>\n<h2 id="system-auto-provided-by-convex">System (auto-provided by Convex)</h2>\n<p>| Variable          | Purpose                                                       |\n| ----------------- | ------------------------------------------------------------- |\n| <code>CONVEX_SITE_URL</code> | HTTP actions URL. Used as JWT issuer and OAuth callback base. |</p>\n<p>Your <code>convex/auth.config.ts</code> should trust this same value as the native Convex\nJWT issuer:</p>\n<pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#D73A49;--shiki-dark:#F47067">import</span><span style="color:#24292E;--shiki-dark:#ADBAC7"> { env } </span><span style="color:#D73A49;--shiki-dark:#F47067">from</span><span style="color:#032F62;--shiki-dark:#96D0FF"> "./_generated/server"</span><span style="color:#24292E;--shiki-dark:#ADBAC7">;</span></span>\n<span class="line"></span>\n<span class="line"><span style="color:#D73A49;--shiki-dark:#F47067">export</span><span style="color:#D73A49;--shiki-dark:#F47067"> default</span><span style="color:#24292E;--shiki-dark:#F69D50"> {</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">  providers: [</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">    {</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">      domain: </span><span style="color:#032F62;--shiki-dark:#96D0FF">`${</span><span style="color:#24292E;--shiki-dark:#ADBAC7">env</span><span style="color:#032F62;--shiki-dark:#96D0FF">.</span><span style="color:#005CC5;--shiki-dark:#6CB6FF">CONVEX_SITE_URL</span><span style="color:#032F62;--shiki-dark:#96D0FF">}/auth`</span><span style="color:#24292E;--shiki-dark:#ADBAC7">,</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">      applicationID: </span><span style="color:#032F62;--shiki-dark:#96D0FF">"convex"</span><span style="color:#24292E;--shiki-dark:#ADBAC7">,</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">    },</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">  ]</span><span style="color:#24292E;--shiki-dark:#F69D50">,</span></span>\n<span class="line"><span style="color:#24292E;--shiki-dark:#F69D50">}</span><span style="color:#24292E;--shiki-dark:#ADBAC7">;</span></span></code></pre>\n<h2 id="provider">Provider</h2>\n<p>| Pattern                  | Example              |\n| ------------------------ | -------------------- |\n| <code>AUTH_&#x3C;PROVIDER>_ID</code>     | <code>AUTH_GITHUB_ID</code>     |\n| <code>AUTH_&#x3C;PROVIDER>_SECRET</code> | <code>AUTH_GITHUB_SECRET</code> |</p>\n<h3 id="oauth-provider-env">OAuth provider env</h3>\n<p>| Provider  | Required variables                                                                   | Optional variables      |\n| --------- | ------------------------------------------------------------------------------------ | ----------------------- |\n| Google    | <code>AUTH_GOOGLE_ID</code>, <code>AUTH_GOOGLE_SECRET</code>                                               | -                       |\n| GitHub    | <code>AUTH_GITHUB_ID</code>, <code>AUTH_GITHUB_SECRET</code>                                               | -                       |\n| Apple     | <code>AUTH_APPLE_ID</code>, <code>AUTH_APPLE_TEAM_ID</code>, <code>AUTH_APPLE_KEY_ID</code>, <code>AUTH_APPLE_PRIVATE_KEY</code> | -                       |\n| Microsoft | <code>AUTH_MICROSOFT_TENANT_ID</code>, <code>AUTH_MICROSOFT_ID</code>                                      | <code>AUTH_MICROSOFT_SECRET</code> |</p>\n<p>OAuth provider callbacks default to:</p>\n<pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span>${CONVEX_SITE_URL}/auth/callback/&#x3C;provider></span></span></code></pre>\n<p>This matches the default <code>path: "/auth"</code> used by <code>defineAuth</code>. Pass\n<code>redirectUri</code> in provider config when you want to override that default.</p>\n<h2 id="optional">Optional</h2>\n<p>| Variable                            | Purpose                                                          | Default           |\n| ----------------------------------- | ---------------------------------------------------------------- | ----------------- |\n| <code>AUTH_SESSION_TOTAL_DURATION_MS</code>    | Max session lifetime                                             | 30 days           |\n| <code>AUTH_SESSION_INACTIVE_DURATION_MS</code> | Inactive session timeout                                         | Provider-specific |\n| <code>AUTH_LOG_LEVEL</code>                    | <code>DEBUG</code> / <code>INFO</code> / <code>WARN</code> / <code>ERROR</code>                              | <code>INFO</code>            |\n| <code>AUTH_LOG_SECRETS</code>                  | <code>"true"</code> logs secret values in full; otherwise they are redacted | <code>"false"</code>         |</p>\n<h3 id="well-known-content"><code>.well-known</code> content</h3>\n<p>These drive the <a href="/convex-auth/reference/well-known/">.well-known endpoints</a> — leave them\nunset to disable a given endpoint (it then returns 404).</p>\n<p>| Variable                    | Purpose                                                             | Default               |\n| --------------------------- | ------------------------------------------------------------------- | --------------------- |\n| <code>IOS_APP_IDS</code>               | Comma-separated <code>TEAMID.bundle.id</code> for <code>apple-app-site-association</code> | -                     |\n| <code>IOS_APPLINK_PATHS</code>         | Comma-separated path patterns for <code>applinks</code> (e.g., <code>/auth/*</code>)      | <code>/auth/*,/callback/*</code> |\n| <code>ANDROID_APP_LINKS</code>         | <code>package:FP1;package2:FP2</code> for <code>assetlinks.json</code>                    | -                     |\n| <code>APP_URL</code>                   | Origin emitted by <code>/.well-known/webauthn</code>                           | -                     |\n| <code>CHANGE_PASSWORD_URL</code>       | Redirect target for <code>/.well-known/change-password</code>                  | -                     |\n| <code>SECURITY_CONTACT</code>          | <code>Contact:</code> for <code>security.txt</code> (<code>mailto:</code> or <code>https:</code>)               | -                     |\n| <code>SECURITY_TXT_EXPIRES_DAYS</code> | Days until <code>Expires:</code> in <code>security.txt</code>                             | 365                   |</p>\n<p><code>APP_URL</code> is the canonical frontend URL used for generated links and default\nredirects:</p>\n<pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#24292E;--shiki-dark:#ADBAC7">APP_URL</span><span style="color:#D73A49;--shiki-dark:#F47067">=</span><span style="color:#032F62;--shiki-dark:#96D0FF">https://app.example.com</span></span></code></pre>\n<h3 id="email-and-password-provider">Email and password provider</h3>\n<p>These are declared on <code>authEnv</code> (so they are typed and validated when you use\n<code>defineApp({ env: authEnv })</code>), but they are read by your own provider config in\n<code>convex/auth.ts</code> rather than by the library directly. Wire them where you\nconfigure the <code>email()</code> and <code>password()</code> providers.</p>\n<p>| Variable                           | Purpose                                                                        | Default   |\n| ---------------------------------- | ------------------------------------------------------------------------------ | --------- |\n| <code>AUTH_EMAIL</code>                       | Default <code>from</code> address for the <code>email()</code> provider                              | -         |\n| <code>RESEND_API_KEY</code>                   | API key for sending email through Resend from your <code>email()</code> provider\'s <code>send</code> | -         |\n| <code>AUTH_PASSWORD_EMAIL_VERIFICATION</code> | <code>"true"</code> enables email verification / reset for the <code>password()</code> provider      | <code>"false"</code> |</p>',
  slug: "/getting-started/environment",
  title: "Environment Variables",
} satisfies DocumentationPage;

export default page;
