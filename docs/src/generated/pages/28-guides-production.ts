import type { DocumentationPage } from "../docs";

const page = {
  description: "Move a Convex Auth application from local development to production.",
  html: '<p>A production deployment uses the same component and application code as local\ndevelopment. What changes is the trust boundary: signing keys must be generated\nfor the production deployment, provider credentials must name production\ncallbacks, and the public application URL must be the origin users actually\nvisit.</p>\n<p>Generate the production key material with the setup CLI:</p>\n<pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">npx</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex-auth</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --prod</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --app-url</span><span style="color:#032F62;--shiki-dark:#96D0FF"> "https://myapp.com"</span></span></code></pre>\n<p>The CLI writes the signing and encryption material to the Convex deployment. Do\nnot copy development keys into production or store them in source control. Set\nthe provider credentials owned by your application through Convex as well:</p>\n<pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">npx</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex</span><span style="color:#032F62;--shiki-dark:#96D0FF"> env</span><span style="color:#032F62;--shiki-dark:#96D0FF"> set</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --prod</span><span style="color:#032F62;--shiki-dark:#96D0FF"> GITHUB_CLIENT_ID</span><span style="color:#032F62;--shiki-dark:#96D0FF"> "..."</span></span>\n<span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">npx</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex</span><span style="color:#032F62;--shiki-dark:#96D0FF"> env</span><span style="color:#032F62;--shiki-dark:#96D0FF"> set</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --prod</span><span style="color:#032F62;--shiki-dark:#96D0FF"> GITHUB_CLIENT_SECRET</span><span style="color:#032F62;--shiki-dark:#96D0FF"> "..."</span></span></code></pre>\n<p>Then deploy the application and its Convex functions using the build command for\nyour app:</p>\n<pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">npx</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex</span><span style="color:#032F62;--shiki-dark:#96D0FF"> deploy</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --cmd</span><span style="color:#032F62;--shiki-dark:#96D0FF"> \'npm run build\'</span></span></code></pre>\n<h2 id="verify-the-trust-chain">Verify the trust chain</h2>\n<p><code>APP_URL</code> must be the production frontend origin. Convex provides\n<code>CONVEX_SITE_URL</code>; <code>convex/auth.config.ts</code> must trust its <code>/auth</code> issuer with\n<code>applicationID: "convex"</code>. Every OAuth provider registration must send its\ncallback to that same production site URL. These values describe one round\ntrip, so a staging origin, localhost callback, or mismatched issuer will fail\neven when each value looks valid in isolation.</p>\n<p><code>AUTH_KEYS</code> must exist on the production deployment, and every enabled provider\nmust receive the credentials that its factory expects. Convex Auth does not use\ngeneric <code>AUTH_*</code> provider variables automatically; the names are the ones your\napplication declared and passed to the provider.</p>\n<h2 id="publish-platform-association-files">Publish platform association files</h2>\n<p>Native passkeys and app links require the frontend host to publish platform\nassociation documents. iOS uses\n<code>/.well-known/apple-app-site-association</code> with no extension and no redirect;\nAndroid uses <code>/.well-known/assetlinks.json</code>. Both must be served from the host\nused as the WebAuthn relying-party ID. Configure <code>IOS_APP_IDS</code> and\n<code>ANDROID_APP_LINKS</code> with the applications that are allowed to claim that host.</p>\n<p>Password-manager integration can publish <code>/.well-known/change-password</code>, and a\nsecurity contact can publish <code>/.well-known/security.txt</code>. These endpoints are\ndescribed in the <a href="/convex-auth/reference/well-known/">.well-known reference</a>, but they should\nbe deployed only when the corresponding application behavior exists.</p>\n<h2 id="understand-refresh-traffic">Understand refresh traffic</h2>\n<p>The browser client periodically exchanges its stored refresh token for a short\nlived access token. Convex logs may show an <code>auth:signIn</code> action followed by an\n<code>auth:store</code> mutation during page load, token refresh, or activity in another\ntab. That pair is normal and does not represent a fresh interactive sign-in.</p>\n<p>A refresh can cause active Convex subscriptions to re-evaluate. If one refresh\nproduces a large burst of query work, look for the same query subscribed in both\na page and its child, auth-dependent queries mounted on routes that do not need\nthem, or queries that should use <code>"skip"</code> until their inputs exist. Pass already\nloaded data down the component tree instead of opening another subscription.</p>\n<p>Repeated refreshes in a tight loop from one tab are different. They usually\nindicate more than one auth client, unavailable or corrupted token storage, or a\nproxy retrying a failed exchange. Diagnose that loop at the client and storage\nboundary rather than adding application-owned session state.</p>',
  slug: "/guides/production",
  title: "Production deployment",
} satisfies DocumentationPage;

export default page;
