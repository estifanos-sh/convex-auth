import type { DocumentationPage } from "../docs";

const page = {
  description: "Deploy convex-auth to production.",
  html: '<h1 id="production-deploy">Production Deploy</h1>\n<h2 id="setup-production-keys">Setup production keys</h2>\n<div><div><p><code>pnpm exec convex-auth --prod --app-url "https://myapp.com"</code></p></div><div><p><code>npx convex-auth --prod --app-url "https://myapp.com"</code></p></div><div><p><code>yarn convex-auth --prod --app-url "https://myapp.com"</code></p></div></div>\n<h2 id="set-provider-secrets">Set provider secrets</h2>\n<div><div><p><code>pnpm exec convex env set --prod AUTH_GITHUB_ID "..."</code> then\n<code>pnpm exec convex env set --prod AUTH_GITHUB_SECRET "..."</code></p></div><div><p><code>npx convex env set --prod AUTH_GITHUB_ID "..."</code> then\n<code>npx convex env set --prod AUTH_GITHUB_SECRET "..."</code></p></div><div><p><code>yarn convex env set --prod AUTH_GITHUB_ID "..."</code> then\n<code>yarn convex env set --prod AUTH_GITHUB_SECRET "..."</code></p></div></div>\n<h2 id="deploy">Deploy</h2>\n<div><div><pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">pnpm</span><span style="color:#032F62;--shiki-dark:#96D0FF"> exec</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex</span><span style="color:#032F62;--shiki-dark:#96D0FF"> deploy</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --cmd</span><span style="color:#032F62;--shiki-dark:#96D0FF"> \'pnpm run build\'</span></span></code></pre></div><div><pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">npx</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex</span><span style="color:#032F62;--shiki-dark:#96D0FF"> deploy</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --cmd</span><span style="color:#032F62;--shiki-dark:#96D0FF"> \'npm run build\'</span></span></code></pre></div><div><pre class="shiki shiki-themes github-light github-dark-dimmed" style="background-color:#fff;--shiki-dark-bg:#22272e;color:#24292e;--shiki-dark:#adbac7" tabindex="0"><code><span class="line"><span style="color:#6F42C1;--shiki-dark:#F69D50">yarn</span><span style="color:#032F62;--shiki-dark:#96D0FF"> convex</span><span style="color:#032F62;--shiki-dark:#96D0FF"> deploy</span><span style="color:#005CC5;--shiki-dark:#6CB6FF"> --cmd</span><span style="color:#032F62;--shiki-dark:#96D0FF"> \'yarn build\'</span></span></code></pre></div></div>\n<h2 id="checklist">Checklist</h2>\n<ul>\n<li><code>APP_URL</code> is set to your production domain (not <code>localhost</code>)</li>\n<li><code>JWT_PRIVATE_KEY</code>, <code>JWKS</code>, and <code>AUTH_SECRET_ENCRYPTION_KEY</code> are set (the CLI\nhandles this)</li>\n<li>Provider secrets (<code>AUTH_*_ID</code>, <code>AUTH_*_SECRET</code>) are configured</li>\n<li><code>CONVEX_SITE_URL</code> is auto-provided by Convex</li>\n<li><code>convex/auth.config.ts</code> trusts <code>env.CONVEX_SITE_URL</code> with\n<code>applicationID: "convex"</code></li>\n<li>OAuth callback URLs are registered with your providers pointing to\n<code>CONVEX_SITE_URL</code></li>\n</ul>\n<h3 id="cross-platform-well-known-files">Cross-platform <code>.well-known</code> files</h3>\n<p>Apps using passkeys, password managers, or native iOS/Android sign-in should\nserve these from the frontend host. See the\n<a href="/convex-auth/reference/well-known/">.well-known reference</a> and the\n<a href="/convex-auth/guides/native-apps/">native apps guide</a>.</p>\n<ul>\n<li>For native iOS passkeys: <code>IOS_APP_IDS</code> set, <code>apple-app-site-association</code>\nreachable at the RP ID host with no redirects, no <code>.json</code> extension</li>\n<li>For native Android passkeys: <code>ANDROID_APP_LINKS</code> set, <code>assetlinks.json</code>\nreachable at the RP ID host</li>\n<li>For password manager UX: <code>CHANGE_PASSWORD_URL</code> set (302 from\n<code>/.well-known/change-password</code>)</li>\n<li>For security disclosure: <code>SECURITY_CONTACT</code> set with unexpired\n<code>Expires:</code> (refreshes every <code>SECURITY_TXT_EXPIRES_DAYS</code>, default 365)</li>\n</ul>\n<h2 id="auth-refresh-and-convex-logs">Auth refresh and Convex logs</h2>\n<p>Convex Auth refreshes stored browser sessions when the Convex client asks for a\nfresh access token. In logs this can show up as an <code>auth:signIn</code> action followed\nby an <code>auth:store</code> mutation. That pair is expected on page load, token refresh,\nand across multiple tabs or visitors.</p>\n<p>Each refresh mutation can cause active Convex subscriptions to re-evaluate. If\nyour logs show a burst of many cached query evaluations after auth refresh, first\nlook for duplicate or unnecessary subscriptions in the app:</p>\n<ul>\n<li>avoid subscribing to the same query in both a page and child component;</li>\n<li>pass already-loaded data as props when possible;</li>\n<li>use <code>"skip"</code> for queries that are disabled by config or route state;</li>\n<li>only run auth-dependent queries on pages that actually need them.</li>\n</ul>\n<p>Treat refresh as suspicious when a single tab with one auth client refreshes in a\ntight loop. Common causes are duplicate auth clients, unavailable storage,\ncorrupted refresh-token storage, or proxy retry failures.</p>',
  slug: "/guides/production",
  title: "Production Deploy",
} satisfies DocumentationPage;

export default page;
